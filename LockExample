pragma solidity ^0.4.24;

contract LockExample {
    bool private lock;   // ← これを検出したい

    function withdraw(uint amount) public {
        require(!lock);
        lock = true;
        // ... 送金処理 ...
        lock = false;
    }
}
